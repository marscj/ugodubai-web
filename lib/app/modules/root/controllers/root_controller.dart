import 'package:get/get.dart';

class RootController extends GetxController {
  // final _delegate = Rx<GetDelegate?>(null);
  // get delegate => this._delegate.value;
  // set delegate(value) => this._delegate.value = value;
}
